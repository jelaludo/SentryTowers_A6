import fs from 'node:fs/promises';
import crypto from 'node:crypto';
import * as T from 'three';
import {GLTFExporter} from 'three/addons/exporters/GLTFExporter.js';
import {NodeIO} from '@gltf-transform/core';
import {ALL_EXTENSIONS,EXTMeshoptCompression} from '@gltf-transform/extensions';
import {dedup,reorder} from '@gltf-transform/functions';
import {MeshoptEncoder,MeshoptDecoder} from 'meshoptimizer';
import {buildModel,definitions,palette} from './settlement-industry-shape.mjs';
const out=new URL('../../assets/settlement-industry/',import.meta.url);
globalThis.FileReader=class{readAsArrayBuffer(b){b.arrayBuffer().then(x=>{this.result=x;this.onloadend?.();});}readAsDataURL(b){b.arrayBuffer().then(x=>{this.result=`data:${b.type};base64,${Buffer.from(x).toString('base64')}`;this.onloadend?.();});}};
await Promise.all([MeshoptEncoder.ready,MeshoptDecoder.ready]);
const io=new NodeIO().registerExtensions(ALL_EXTENSIONS).registerDependencies({'meshopt.encoder':MeshoptEncoder,'meshopt.decoder':MeshoptDecoder});
const sha=b=>crypto.createHash('sha256').update(b).digest('hex'),assets=[];
for(const [family,d] of Object.entries(definitions))for(const lod of [0,1,2]){
 const root=buildModel(family,lod);root.updateMatrixWorld(true);let triangles=0,draw_calls=0;const sockets=[],engine_nodes=[],lookup_nodes=[];
 root.traverse(o=>{if(o.isMesh){triangles+=(o.geometry.index?.count||o.geometry.attributes.position.count)/3;draw_calls++;}else lookup_nodes.push(o.name);
  if(o.userData.role==='engine_pivot')engine_nodes.push(o.name);
  if(o.userData.role==='socket')sockets.push({id:o.name.replace('SOCKET_',''),node:o.name,kind:o.userData.kind,interface:o.userData.interface,position_m:o.getWorldPosition(new T.Vector3()).toArray(),normal:new T.Vector3(0,0,1).applyQuaternion(o.getWorldQuaternion(new T.Quaternion())).toArray()});
 });
 const b=new T.Box3().setFromObject(root,true),file=`lod${lod}/${family}_d0_lod${lod}.glb`;await fs.mkdir(new URL(`lod${lod}/`,out),{recursive:true});
 const bytes=Buffer.from(await new GLTFExporter().parseAsync(root,{binary:true,trs:true}));await fs.writeFile(new URL(file,out),bytes);
 const doc=await io.readBinary(bytes);await doc.transform(dedup(),reorder({encoder:MeshoptEncoder,target:'size'}));doc.createExtension(EXTMeshoptCompression).setRequired(true).setEncoderOptions({method:EXTMeshoptCompression.EncoderMethod.FILTER});
 const packed=await io.writeBinary(doc),meshopt_file=`derived/meshopt/lod${lod}/${family}_d0_lod${lod}.meshopt.glb`;await fs.mkdir(new URL(`derived/meshopt/lod${lod}/`,out),{recursive:true});await fs.writeFile(new URL(meshopt_file,out),packed);
 const e={id:`${family}_d0_lod${lod}`,family,label:d.label,file,lod,damage_level:0,static:lod===2,derived:false,bytes:bytes.length,sha256:sha(bytes),triangles,draw_calls,materials:family==='mirror'&&lod<2?2:1,textures:0,meshopt_file,meshopt_bytes:packed.length,meshopt_sha256:sha(packed),meshopt_derived:true,plot_m:d.plot,bounds:{min:b.min.toArray(),max:b.max.toArray(),dimensions_m:b.getSize(new T.Vector3()).toArray()},swept_envelope_m:root.userData.swept_envelope_m||null,sockets,clips:[],engine_nodes,required_nodes:[d.root,...engine_nodes,...sockets.map(s=>s.node)],credit:'Models by jelaludo'};assets.push(e);console.log(e.id,triangles,'triangles',draw_calls,'draws',bytes.length,'bytes');
}
const manifest={schema:'jelaludo.asset-family/v2-candidate',version:1,family:'settlement-industry',label:'Frontier industry / ISAO–MÖRK–KORP lineage',status:'contract_candidate_pending_game_camera_reference_phone',plain_glb_source_of_truth:true,coordinate_system:{units:'meters',up:'+Y',forward:'+Z',origin:'ground plot centre; operating envelopes may extend beyond the chassis'},supported_damage_levels:[0],missing_damage_states_reason:'First intact vertical slice; D1–D3 not authored.',manufacturer:{lineage:['ISAO-Birudoron','MORK','KORP GS01'],name:'Existing shared manufacturer; no new corporation name introduced',palette:Object.fromEntries(Object.entries(palette).map(([k,v])=>[k,'#'+v.toString(16).padStart(6,'0')])),features:['chamfered petrol/slate shells','pale inset armor','black mechanical recesses','cyan functional readouts','amber service markers']},budgets:{landmark:{lod1:{triangles:8000,draw_calls:10,bytes:400000},lod2:{triangles:3000,draw_calls:1,bytes:250000}},unit:{lod1:{triangles:25000},lod2:{triangles:3000,draw_calls:1,bytes:250000}},base_kit:{lod1:{triangles:1500,draw_calls:5},lod2:{triangles:1500,draw_calls:1,bytes:250000}}},lod_selection:{detailed:'Manual recording/close-shot use only',landmark:{initial:2,approach_distance_m:150,hysteresis_m:20,provisional:true},hauler:'Retain articulated LOD1 for active vehicles. LOD2 is an optional static parked/loading proxy; never swap during loading or travel.',cassette:'Use LOD1 for interaction; optional static LOD2 for large logistics yards.',distance:'All visible geometry merged to one static mesh/material. Retained controls are lookup nodes only. Mirror optics become a pale opaque surface.',review:'Tune thresholds with consuming-game camera; no FPS measurement or improvement claim.'},cargo_interface:{id:'INDUSTRY_CASSETTE_A',size_m:[1.65,1.3525,2.2],origin:'cassette bottom centre',attachment:'Align CAS_ROOT / SOCKET_CARGO to carrier SOCKET_CARGO; do not scale',dock_clearance_review:'Geometric fit validated in the supplied demo; terrain/physics/game integration pending'},runtime:{adapter:'runtime.js',baked_clips:[],engine_driven:'Drill mast, anchors, feed/spin and ore chute, truck suspension/steering/wheels/sensor/lift/slide, mirror yaw/pitch and furnace hatch remain unbaked.',demonstration:{name:'Extraction_Collection',duration_s:32,loop:false,events:[{time_s:0,event:'DRILL_START'},{time_s:10,event:'CASSETTE_READY'},{time_s:11,event:'CHUTE_CLEAR'},{time_s:15,event:'LIFT_AT_BED'},{time_s:19,event:'CARGO_STOWED'},{time_s:21,event:'DEPART'},{time_s:29,event:'DELIVERY_STOP'},{time_s:32,event:'COMPLETE'}]},scope:'Kinematic presentation only; inventory, navigation, collision and resource processing require consuming-game integration.',solar:'Mirror normals bisect direction to sun and receiver. Optical rays are optional explanatory viewer guides, not visible laser beams or a thermal simulation.'},assets};
await fs.writeFile(new URL('manifest.json',out),JSON.stringify(manifest,null,2)+'\n');
